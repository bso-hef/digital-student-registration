# MongoDB 7.0 Configuration for Docker Production
# Minimal, production-ready configuration

net:
  port: 27017
  bindIp: 0.0.0.0

storage:
  dbPath: /data/db

security:
  authorization: enabled

# Logging defaults to stdout (container best practice)
# No systemLog configuration needed

processManagement:
  fork: false
